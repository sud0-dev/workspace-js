{
  "name": "workspacejs",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:rust-repl": "bun install --frozen-lockfile && bun --filter rust-repl build",
    "build:portfolio": "bun install --frozen-lockfile && bun --filter portfolio build",
    "build:crates":    "bun install --frozen-lockfile && bun --filter crates build",
    "deploy:rust-repl": "bun run build:rust-repl && bun --filter rust-repl run deploy:wrangler",
    "deploy:portfolio": "bun run build:portfolio && bun --filter portfolio run deploy:wrangler",
    "deploy:crates":    "bun run build:crates    && bun --filter crates    run deploy:wrangler",
    "dev:rust-repl": "bun --filter rust-repl dev",
    "dev:portfolio": "bun --filter portfolio dev",
    "dev:crates":    "bun --filter crates    dev",
    "test": "bun --filter '*' run test",
    "typecheck": "bun --filter '*' run typecheck"
  }
}
